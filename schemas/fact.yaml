Fact:
  type: object
  properties:
    id:
      type: string
      format: uuid
    create_at:
      type: string
      format: date-time
    updated_at:
      type: string
      format: date-time
    title:
      type: string
    author_id:
      type: string
      format: uuid
    author_name:
      type: string
    references:
      type: array
      items:
        $ref: "../schemas/reference.yaml#/Reference"